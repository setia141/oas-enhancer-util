# ── Required ──────────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-...

# ── Optional ──────────────────────────────────────────────────────────────────
# Override if using a corporate AI gateway instead of api.openai.com
# OPENAI_BASE_URL=https://your-gateway.example.com/v1

# Model and token limits — tune for your model's output token cap
# gpt-4.1-mini  → SUGGESTER_MODEL=gpt-4.1-mini  BATCH_TOKENS=32768  BATCH_SIZE=100
# gpt-5-mini    → SUGGESTER_MODEL=gpt-5-mini     BATCH_TOKENS=120000 BATCH_SIZE=500
# SUGGESTER_MODEL=gpt-4.1-mini
# BATCH_TOKENS=32768
# BATCH_SIZE=100
